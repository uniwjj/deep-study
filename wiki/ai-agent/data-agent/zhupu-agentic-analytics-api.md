---
title: 舟谱数据：Agentic 数据分析与 API 生产实践
description: 舟谱数据（快消流通领域数智服务商）自研 zp-data-works 智能数据平台——为什么普通 Text2SQL 不够用（语义/安全/交付三道工程鸿沟），步步有证据的 Agent 链路：Skill 确定口径（296 指标/33 模块/17 维度）、六层权限织入、一键生成 API 草稿、有界 Context
aliases: [舟谱数据, zp-data-works, Agentic 数据分析 API 生产]
tags: [ai-agent, big-data, platform, practice]
sources: [2026/08/11/Agent 驱动的数据智能新范式(阿里云专场)论坛/03-周泽启-从数据平台到智能数据助手：Agentic数据分析与 API生产实践.pdf]
created: 2026-08-11
updated: 2026-08-11
---

# 舟谱数据：Agentic 数据分析与 API 生产实践

2026-08-11 2026 Agent 大会「Agent 驱动的数据智能新范式（阿里云专场）论坛」演讲。演讲人：**周泽启**｜舟谱数据智能数据平台。主题：从数据平台到智能数据助手——Agentic 数据分析与 API 生产实践。核心论点：**卡住 Text2SQL 的是三道工程鸿沟（语义鸿沟、安全鸿沟、交付鸿沟），模型能力反而不是瓶颈**。

## 摄取说明

PDF 共 26 页，已用文本层提取 + 关键架构图页（P3）与界面演示页（P19-P25）转图 OCR 视觉复核；架构图连线细节无法完全辨认的部分未摄取。

## 公司介绍与整体架构

舟谱数据：专注**快消流通领域**的数智服务商，助力企业提升全链路运营与流通效率。产品 zp-data-works：一体化数据服务与 AI 数据开发平台（ONE-STOP DATA SERVICE & AI DATA PLATFORM），价值：降低数据开发门槛、数据可视化、业务口径统一。

整体架构（P3 架构图，OCR 复核）：

- **采集与实时接入层**：Kafka、RDS MySQL、Photon（Photon 从 RDS MySQL 采集 Binlog 并发送到 Kafka）、全托管 Flink
- **湖仓加工层**：ODS（ODS 层表）、Hologres（动态表加工、维度表）、MaxCompute
- **服务与数据服务层**：数据服务/指标——提供参数化指标调用 / 基于指标调用的数据服务（OCR 无法完全辨认的打包形式标注未摄取）
- 上层为 zp-data-works 智能数据平台

## 为什么普通 Text2SQL 不够用：三道工程鸿沟

- **GAP 01 语义鸿沟**：业务词如何对应正式指标口径？口径错一位，结论就失去可信度
- **GAP 02 安全鸿沟**：可执行 SQL 如何保证只访问允许的数据？租户 ID 是访问边界，不是普通筛选条件
- **GAP 03 交付鸿沟**：一次查询如何沉淀成可复用的数据服务？答案停在代码块，价值就停在对话里

## 项目全景：步步有证据的 Agent 链路

```
业务问题 → Agent 规划（DAG 调度）→ 指标解析 Agent → SQL Agent → 权限内执行（真实取证）→ API Agent（草稿交付）
```

三个关键支撑：

- **有界 Context 快照**：知识 · 记忆 · Capsule · Artifact
- **版本化 Skill 快照**：296 指标 · 33 模块 · 17 维度
- **MCP 网关 + 只读数据库身份**：执行计划权限传播

原则：**结构化传递 · 每步有证据 · 上下文有界**——Agent 之间传递结构化产物；每个结论都有可验证证据；上下文按相关性装配、大小受控。

## 四大优势

### 优势一：Skill 让 Text2SQL 更可信

Skill = 反向浓缩的业务资产，可装载 · 可版本化的指标知识快照：**296 正式指标、33 数据模块、17 维度定义**。快照内容：正式指标名称 · 表达式 · 指标依赖；指标 → 数据模块的路由关系；模块层级与最深模块选择规则；维度表 · 关联键 · 维度展开关系；时间口径 · 过滤条件 · SQL 方言；原子/复合指标的组装规则。

两条独立路径，语义等价对比：

- 路径 A：自然语言 → Skill 约束 Text2SQL → 候选 SQL
- 语义等价对比：表达式 / 模块 / 过滤 / 粒度 / 依赖
- 路径 B：指标名称 + 参数 → 原指标项目参数化调用 → 基准 SQL

不确定时不猜：**保持原词 · 候选澄清 · 未找到即拒答 · 解析失败即阻断下游**。

### 优势二：权限是执行链路的一部分（纵深防御）

权限织入执行链路的每一层：

1. **指标与 Skill 层**：生成阶段显式带入租户与关联约束
2. **Agent 策略层**：单条 · 可解析 · 只读，拒绝写入与副作用
3. **项目 SQL 检查层**：AST 遍历 JOIN / 子查询 / UNION（告警）
4. **MCP 接入层**：加密数据源信息 + _permission_mode
5. **MCP 网关层**：按执行计划传播租户 ID 边界（由 MCP 项目承担）
6. **数据库层**：只读账号，最终不可绕过的边界

为什么不依赖模型自觉：风险从哪来——主表已限租户 ID，JOIN 表未纳入关联条件，就可能带入其他租户记录；UNION 任一分支缺约束，结果即混入越权数据。防线怎么分工——项目侧尽早规范，MCP 与数据库承担执行期隔离，权限不依赖模型自觉。金句：「SQL 里写了租户 ID 还不够，数据沿执行计划流动时，也不能离开租户 ID 边界。」

### 优势三：从一次查询到 API 草稿（一键交付）

同一份业务意图与已验证 SQL，自动搬运成 API 草稿，人保留发布决策。流程：01 参数识别 → 02 动态化改写（租户 ID/时间/门店/品牌参数，固定值改造为 MyBatis 动态条件，草稿测试通过后创建）→ 03 分层 AST 检查 → 04 MCP 可执行验证 → 05 草稿分页测试 → 06 创建未发布草稿。创建的是**默认未发布的 API 草稿：可编辑 · 可测试 · 可版本化**。

"边界仍在，搬运消失"——这三道边界，曾经都要靠人跨角色搬运：

- 知识边界：业务口径无需后端重新猜测
- 工具边界：不在多个工具与平台间来回搬运
- 责任边界：每步都有结构化输入与验证证据

### 优势四：有界 Context 与结构化裁剪

按相关性装配，大小受控——知识 · 记忆 · 会话，三类上下文各司其职：

- **知识（组织知道什么）**：EXACT / 词法 / 向量并行召回；加权 RRF 融合，数量字符双上限；长 SQL 给 unitId，只读工具取回
- **记忆（用户长期在意什么）**：category/key/title/content；≤ 8 条 · 单条 ≤ 500 字；支持新增、合并、替代与失效治理
- **会话（事情做到哪里）**：最近消息 + 滚动摘要 + Capsule；Capsule 只存目标 / 决定 / 未决；不信任客户端无限上送的历史

大结果走 **Artifact 安全引用**，裁剪按完整 item 进行，SQL / JSON 不被切断。所有取舍，都由 **Context Manifest** 记录。

## 界面演示

界面演示 7 页（PRODUCT TOUR 1-7，PPT 截图页）：复杂问题 → 问题详情·一 → 问题详情·二 → API 界面 → API 优化 → 数据看板 → Skill 界面。收尾语："让 Agent 不止会写 SQL"。

## 相关页面

- [[dataworks-data-agent]] — DataWorks Data Agent（同场论坛，企业级大数据智能体）
- [[data-agent-practice-guide]] — 火山引擎数据智能体实践指南
- [[code-agent-vs-data-agent]] — Code Agent 与 Data Agent 的根本差异
- [[dataagent-semantic-layer]] — DataAgent 语义层
- [[ontological-semantic-layer]] — 本体化语义层
- [[hologres-agentic-platform]] — Hologres Agentic 多模态检索分析平台（技术栈使用 Hologres/MaxCompute）
- [[hologres-skills]] — Hologres CLI/Skills/AI Function
- [[agent-mcp-protocol]] — MCP 协议基础
