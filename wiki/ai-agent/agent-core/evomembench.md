---
title: EvoMemBench——AI Agent 记忆能力评测基准与实践
description: 浙江创邻×港科大（广州）联合研发的 Agent 自演化记忆评测基准：知识/执行×任务内/跨任务四象限（INEP-KNOW/INEP-EXEC/CROSSEP-KNOW/CROSSEP-EXEC），6 个 Benchmark、约 5.8K 样本；结果洞察——直接读原文>外部Memory、简单题被Memory干扰、上下文越短Memory越有价值、能找回≠能更新、迁移匹配决策过程；Memory 建设五原则
aliases: [EvoMemBench, INEP-KNOW, INEP-EXEC, CROSSEP-KNOW, CROSSEP-EXEC, Agent Memory评测, 自演化记忆]
tags: [ai-agent, concept, summary]
sources: [2026/08/11/Agent记忆工程论坛/04-童冰-从“完成任务”到“持续进化” AlAgent 记忆能力的评测与实践.pdf]
created: 2026-08-11
updated: 2026-08-11
---

# EvoMemBench——AI Agent 记忆能力的评测与实践

**演讲**：童冰，浙江创邻科技有限公司 高级算法工程师（2026 Agent 大会「Agent记忆工程论坛」，DataFun AGENTIC AI 超级智能体系统架构峰会，31 页）

主题《从"完成任务"到"持续进化"》。开场对比：Agent A 第一天就很强（每次都从当前上下文重新开始；能完成任务，但不会积累经验；犯过的错误仍可能再次发生）vs Agent B 一开始没那么强（能吸收反馈；能更新知识和状态；能复用有效经验；随着服务时间增长而改善）。**"一次做对，是能力；做过之后变得更好，才是进化。"**

目录：01 Agent Memory 的能力边界；02 EvoMemBench 的设计与方法；03 Benchmark 结果洞察。

## 01 Agent Memory 的能力边界

### 为什么 Agent 开始需要 Memory

Agent 演进三段（图中第三段名称 OCR 不清）：01 Copilot——帮人完成片段工作（一次回答；人负责流程，AI 提供辅助；记忆主要在人脑里，AI 只需要理解当前问题）；02 Task Agent——完成一个闭环任务（一次任务一个流程；AI 能规划、调工具、执行任务）；03 系统级 Agent——持续服务用户、流程和组织（一个用户；需要沉淀知识、更新规则、复用经验；需要短期状态，知道当前任务做到哪一步）。**"Agent 承担的时间跨度越长，Memory 越从'加分项'变成'基础设施'。"**

### 长上下文、RAG 与 Memory 的区别

| 对比维度 | 长上下文 | RAG | Agent Memory |
|---------|---------|-----|--------------|
| 核心作用 | 带着当前任务的完整过程 | 需要时去知识库查资料 | 把重要信息和经验留到以后使用 |
| 主要内容 | 对话、工具结果、中间步骤 | 文档、知识库、外部资料 | 用户偏好、业务状态、历史经验 |
| 使用范围 | 当前会话或当前任务 | 当前任务需要时反复查询 | 跨会话、跨任务长期使用 |
| 如何变化 | 随对话不断追加 | 知识库单独维护，按需读取 | 随交互持续写入、更新和失效 |

"长上下文记住'刚刚发生了什么'，RAG 查询'外部资料怎么说'，Memory 保存'以后还要用什么'。" 例子（"帮我退掉刚才那笔海外订单"）：长上下文回答"刚才那笔"是哪一笔、前面完成了哪些工具调用和执行步骤；RAG 提供当前有效的海外跨境退款政策和合规要求；Memory 提供客户历史偏好、合规审核状态、这次经验未来是否值得复用。**三者通常是协作关系，而不是替代关系。**

### 用四个问题理解 Agent Memory

1. 记什么信息？——用户偏好、业务规则、工具结果、任务状态
2. 记成什么形态？——原始日志、摘要、向量、图、流程、技能
3. 何时使用？——当前任务、同一用户、同类业务、Agent 团队
4. 如何被治理？——来源、版本、权限、失效、删除、安全

"Memory 的难点不是'存下来'，而是判断什么该记、怎么更新、何时使用、何时失效。"

### Agent Memory Benchmark 演进

**从"记住"到"行动"（三关）**：第1关 开卷找答案（过去说过什么？——长对话事实召回）；第2关 纠错改答案（新旧信息冲突时信谁？——更新、修正、选择性遗忘）；第3关 带着记忆做事（记住的信息能不能指导行动？——工具调用、环境交互、任务执行）。

**从"行动"到"持续学习"（三关）**：第4关 复盘提经验（做过的任务能不能帮未来任务？——跨任务知识与执行经验复用）；第5关 自己做笔记（连续信息流里什么值得记？——主动写入、证据选择、未来复用）；第6关 长期可治理（记忆越积越多还能安全吗？——成本、延迟、删除、隐私、污染防御）。

### 现有 Benchmark 缺了什么

现有工作（Lifelong/Continual Memory、Agentic Memory 等）分布在"知识记忆/执行记忆 × 任务内/跨任务"矩阵上。三个断点：
- **01 能力断点**："记住"和"行动"经常分开测——有些 Benchmark 只考事实召回或只考任务执行，很难判断记忆是否真正改善了行动
- **02 时间断点**："当前任务"和"未来任务"经常分开测——单任务内强调状态保持，跨任务强调经验积累，缺少统一视角描述两者之间的关系
- **03 比较断点**：不同结果之间难以横向比较——使用不同任务和模型，Memory 更新时机、上下文预算和指标不同

"我们缺的不是更多测试集，而是一套能够同时描述'知识、行动和时间演化'的统一坐标系。"

## 02 EvoMemBench 的设计与方法

### 项目背景

联合研发：浙江创邻科技有限公司（图技术与企业级 AI 基础设施实践）× 香港科技大学（广州）（Agent Memory 方法与评测研究）。产学研深度融合，共同推进 Agent 记忆系统与评测标准的发展。

### EvoMemBench 自演化记忆定义

Agent 在交互中持续保存、更新、组织和复用信息，让未来决策因为过去经验而改善。

| | Knowledge 知识（记住并修正信息） | Execution 执行（跟踪执行状态） |
|--|-------------------------------|------------------------------|
| **In-Episode 任务内** | 用户改口、规则变化、事实冲突（例：用户先说"普通发票"，后来改成专票） | 工具结果、当前进度、未完成步骤（例：用户说"把刚才那个订单金额改一下"） |
| **Cross-Episode 跨任务** | 积累可复用知识：用户偏好、业务规则、领域知识（例：长期记住某客户的偏好和业务规则） | 复用执行经验：流程策略、失败恢复、操作习惯（例：多次退款后总结出更稳定的处理流程） |

"自演化记忆不只回答'过去发生了什么'，还要回答'现在该更新什么、未来能复用什么'。"

### 四个象限

**象限一：任务内知识演化（INEP-KNOW）**——记住历史并不难，难的是在历史发生变化后，仍然回答"现在什么是真的"：
- 例子1 用户直接改口：用户"出差喜欢靠窗座位"→"改选靠过道"。期望：当前偏好靠过道、靠窗失效；常见错误：两者都记但不辨有效性、只记录丢历史。考核指标：Revision（变化后能否正确维护当前有效状态）、Retention（早期有效信息还能否被找回）
- 例子2 业务规则更新：旧规则"超 5 万经理审批"；新通知"超 3 万必须财务审批"；当前申请 4 万元。期望：应用新规则、需财务审批；常见错误：仍用旧规则、视为补充不处理冲突、忽略时间有效性
- 例子3 多跳信息修正：早期"Alpha 负责人是王经理；王经理的预算由李总批"，更新"Alpha 负责人变为赵经理；赵经理的预算由陈总批"，提问"Alpha 预算谁批？"——更新一条实体关系，会影响后续的推理链

**象限二：任务内执行演化（INEP-EXEC）**——执行记忆最怕的不是忘掉一段话，而是丢失实体、参数和中间状态：
- 连续工具调用故事（修改旅行订单）：Step1 查下周一去上海航班（调工具获三个结果）→ Step2 用户"订最早的那个"（需记："那个"是哪个、日期、路线）→ Step3 只剩商务舱，用户"换第二个，用刚才的乘机人"（需恢复：第二个航班、原乘机人、已选日期）→ Step4 用户"把刚才那个订单改成周二"（歧义：是搜索结果、失败订单还是成功订单）
- 重构一：显式参数改成隐式引用（原始："修改订单 A12345 的日期"→ 重构："修改刚才那个订单"）
- 重构二：复杂任务拆成依赖子任务（搜索→选择→创建→根据结果调整→修改）
- 其他例子：文件系统（建文件夹，把刚下的俩文件移进去，较大的重命名；需记：哪俩文件、哪个大、路径）；交易工具（卖出刚查股票的一半，低于目标价则撤销剩余；需记：代码、查询结果、下单状态）
- "如果 Agent 不知道'刚才那个'指什么，再强的工具能力也无法完成任务。"

**象限三：跨任务知识演化（CROSSEP-KNOW）**——考的不是它原来懂多少，而是给它一套陌生规则后，它能不能越做越熟。向 Agent 提供预训练不可能知道的新制度：规则1 P1 级员工乘坐高铁不得选择商务座；规则2 连续出差超 5 天可报销一次洗衣费；规则3 海外住宿上限提高 30%；规则4 周末返程需单独填写特殊说明。示例：P1 去北京能买商务座吗？（考察找规则1）；P2 去新加坡 6 天，住宿上限多少、报洗衣费吗？（组合海外与时长规则）；Episode 3 程序性知识（周日从客户现场返程需什么材料——规则转流程）；Episode 4 经验规律（提供历史案例如提前订票便宜、展会超标，让 Agent 判别新方案）。**为什么不是普通 RAG？** RAG 只是重新检索制度；要测的是 Agent 是否逐步积累规则、形成可复用知识、不会把特例当通用规则。

**象限四：跨任务执行演化（CROSSEP-EXEC）**——做过一百次不一定叫学习；知道哪些经验可以复用、哪些不能，才叫进化：
- 例子1 工具调用经验：过去"改驾驶模式失败（车在行驶）"→经验"先查状态→停车→修改→验证"；新任务"调能量回收等级"。可复用：改配置前先检查并满足前置状态的"执行模式"
- 例子2 Web Search 经验：过去"查去年发多少产品，仅搜一条新闻出错"→经验"官网→年报→交叉验证"；新任务"查另一公司今年进几国市场"。可复用：寻第一方来源、交叉验证、不信单一结果的策略
- 例子3 具身任务经验：过去"加热番茄放冰箱"→经验"定位→取出→微波加热→开冰箱→放入"；新任务"冷却已加热的苹果"。可复用：抽象出操作顺序，具体位置和容器随任务变化
- 反例（当经验导致失败）：过去"国内订单先退款，再关工单"；新任务"跨境退款（必须先合规审核）"。经验 + 轨迹复制会失败；真正提炼的是适用条件和边界

### 数据集选择的三项原则

1. **必须真正依赖 Memory**：好任务——用户说"修改刚才创建的订单"，参数只能从历史恢复；坏任务——题目已经把订单号、日期和用户信息全部重新给出。若没有 Memory 也能完成，就无法说明 Memory 的价值
2. **尽量只测 Memory**：好任务——把复杂任务拆成连续子步骤，主要考状态跟踪；坏任务——同时要求高难度规划、陌生工具调用、复杂数学推理。失败后无法判断到底是 Memory、规划还是模型能力的问题
3. **覆盖不同可复用结构**：对话（事实/偏好）、工具（参数/状态）、Web（搜索验证）、具身（动作/环境）。单一任务上的最优 Memory，不一定适用于其他业务

### 全景数据图

4 种演化能力、6 个 Benchmark、约 5.8K 个评测样本：

| Benchmark | 对应演化能力 | 规模（样本数） |
|-----------|------------|--------------|
| INEP-KNOW | 任务内知识 | 2,800 |
| INEP-EXEC | 任务内执行 | 800 |
| CROSSEP-KNOW | 跨任务知识 | 884 |
| CROSSEP-TOOL | 跨任务工具执行 | 800 |
| CROSSEP-WEB | 跨任务 Web 执行 | 270 |
| CROSSEP-EMB | 跨任务具身执行 | 200 |

## 03 Benchmark 结果洞察

### 模型加上 Memory，真的更会记了吗？

**结论：在任务内知识演化中，历史放得下时，直接读原文 > 外部 Memory。**

看原文 VS 外部 Memory（准确率越高越好、Rank 越低越好）：

| 方法 | 路线 | Event QA | FC-MH | FC-SH | Overall Rank |
|------|------|----------|-------|-------|--------------|
| Gemini-3-Flash | 看原文 | 98.2 | 58.0 | 96.0 | 1.00 |
| GPT-5-mini | 看原文 | 80.6 | 24.0 | 77.0 | 3.00 |
| BM25 | 外部Memory | 88.8 | 7.0 | 54.0 | 4.17 |
| Mem0 | 外部Memory | 79.2 | 7.0 | 49.0 | 4.50 |
| A-MEM | 外部Memory | 91.2 | 5.0 | 35.0 | 4.83 |
| DeepSeek-V3.2 | 无Memory | 83.0 | 10.0 | 65.0 | 5.50 |

3 项关键指标 Gemini vs 外部 Memory（蓝=直接读原文，橙=外部 Memory 最高值）：Event QA（找回历史事件）98.2 vs A-MEM 91.2（差 +7.0）；FC-SH（单步事实修正）96.0 vs BM25 54.0（差 +42.0）；FC-MH（多跳事实修正）58.0 vs BM25/Mem0 7.0（差 +51.0）。

### 简单题可能被 Memory 干扰，难题才拉开差距

13 种 Memory 方法在不同难度下的表现：Easy（简单）——超过 Baseline 方法数 0/13，13 种方法平均变化 **-6.09**；Hard（困难）——3/13，平均变化 **+7.12**。为什么简单题加 Memory 反而更差：当前输入明确提示"普通员工由直属经理审批"，而 Memory 召回历史"海外出差需要财务审批、金额超 3 万需要副总裁审批、某高管出差曾由 CEO 审批"→ 信噪比下降，Agent 开始怀疑正确答案。"当模型已经知道答案时，Memory 最容易成为噪声。目标不是最大化召回，而是在存在能力缺口时介入。"（二次统计自 Table 5：在简单任务上，没有一种 Memory 方法超过无 Memory 的基线。）

### 上下文越短，Memory 越能补齐执行状态

**结论：历史越容易被截断，Memory 保存"做到哪一步、参数是什么、下一步该做什么"的价值越明显。** 超过无 Memory 基线的方法数：16K 上下文 **11/15**、32K **1/15**、64K **12/15**、128K **9/15**（各长度对应提升数值 +14.5、+8.5、+4.13、+3.07、+0.70 等，OCR 对应关系部分不确定）。

### 能找回旧信息 ≠ 能更新旧结论

修订难题场景还原：1月"用户喜欢靠窗座位"；6月"用户腿部受伤，改为靠过道"。Retention 平均表现 **51.6%**，Revision 平均表现 **18.1%**。系统应该如何处理？A. 删除"靠窗"；B. 同时保留两条（大部分 Memory 的做法，导致冲突）；C. 保存变化历史，但将"靠过道"设为当前有效。为什么 Revision（修正）这么难——处理链路拆解：1. 找到相关记忆（容易 Search）→ 2. 识别实体同一性 → 3. 判断冲突或补充 → 4. 判断优先级与时间 → 5. 更新/降权/失效（极难 Update）。

### 没有最好的 Memory，只有更匹配的 Memory

Memory 范式 × 任务类型的 Family Average Rank（KFA，越低越好）：

| Memory 范式 | 工具调用 (Tool) | Web Search | 具身任务 (Embodied) |
|------------|----------------|------------|--------------------|
| 检索型 Memory | 5.43 | 8.50 | （OCR 不清） |
| 通用长期 Memory | 8.22 | 5.38 | （OCR 不清） |
| Meta-Evolution | 8.80 | 6.00 | （OCR 不清） |

- 工具调用：相似案例反复出现，可复用结构是 API 名称与参数模式、调用约束与前置状态 → 召回相似历史有效
- Web Search：需积累验证习惯，可复用结构是哪类来源更可信、如何交叉验证 → 长期经验管理更有价值
- 具身任务：流程和环境规律重要，可复用结构是动作顺序及前提状态、失败后的恢复路径 → Procedure & Skill 匹配

### 迁移不是复制经验，而是匹配决策过程

Tool Using：更多正迁移；Embodied AI：正负混杂（各环境迁移收益矩阵，数字 OCR 部分不确定）。共享决策步骤：检查状态 → 选择下一次工具调用 → 验证最终状态；环境差异更大：动作前提、物体位置、执行条件都可能变化。**"真正可迁移的不是'这次做过什么'，而是'这类任务的决策过程'。"**

### 从 Benchmark 结果到 Memory 建设的五条原则

1. **先建立强 Baseline**：先比较强模型、长上下文和简单检索，再证明复杂 Memory 的增量价值
2. **不要默认每次都使用 Memory**：当前证据足够时，额外 Memory 可能降低信噪比，应该只在存在能力缺口时介入
3. **把更新和失效作为一等能力**：企业 Memory 的核心不只是 Add 和 Search，更是 Update、Invalidate 和 Rollback
4. **区分"经验抽象"和"精确状态"**：Procedure 可以压缩抽象，但订单号、文件路径和工具结果等现场状态必须精确保留
5. **为经验记录适用条件**：历史成功不等于当前适用，跨任务复用必须通过适用性和前提约束控制负迁移

**Memory 净价值 = 任务收益 - Token 成本 - 检索延迟 - 维护成本 - 迁移风险 - 安全风险**

## 公司背景（浙江创邻）

浙江创邻科技有限公司：国内领先的图数据库与图智能服务供应商（2016 年成立，国家引才项目落地杭州；2021 年打破 LDBC SNB 世界纪录，获得腾讯、同创伟业、达晨财智过亿元投资；国家高新技术企业、国家专精特新"小巨人"企业；金融市场份额全国第一；出版《图数据库理论与实践》）。产品线：图平台 Galaxybase（面向"实体—关系—路径—网络"数据结构的国产高性能分布式数据库，打破 LDBC SNB 测试世界纪录、打破万亿大图世界纪录，国内最早商业化图平台）、行业图谱方案、图智能平台 Graph Intelligence（自主建模、图模式识别、图特征计算与图算法分析）、GraphRAG / HybridRAG（融合向量检索、关键词检索与图谱关系检索的企业级检索增强系统）、AI OS 行业智能体（情报分析、故障成因分析、科研项目查重、客服智能问答等）、GraphoraX 企业 AI 大脑（企业"数字员工中枢"，从"对话交互"到"任务执行"的范式升级）。2024 年发布 GraphoraX 平台、获评国家专精特新"小巨人"；2025 年发布 AI 一体机、布局具身智能场景；2026 年发布企业级小龙虾（产品名，OCR 原样）。

## 相关页面

- [[agent-evaluation-framework]] — AI Agent 测评框架
- [[agent-memory-system]] — Agent 长短期记忆系统基础概念
- [[lycheememory-memory-scaling]] — LycheeMemory 记忆系统与记忆前沿工作综述（含 LoCoMo 等评测）
- [[oppo-multimodal-agent-memory]] — OPPO 多模态 Agent 记忆实践
- [[mem0]] — 跨会话用户记忆层（EvoMemBench 中作为被测方法之一）
- [[business-cognition-system]] — 可演进的业务认知系统（知识更新与失效治理）
