---
title: 高确定性商业场景下的 Agent 落地实践（运满满找货 AI）
description: 运满满找货 AI 三大工程实践——事件驱动按需唤醒（不轮询=不花token）、意图编译为 WatchPlan 规则脚本（快慢双 Loop）、5 级动作分层授权——让 Agent 做判断，工程保证执行的确定性
aliases: [high-certainty-agent-practices, 运满满找货AI, 满帮找货Agent, WatchPlan, 事件驱动Agent, 意图编译]
tags: [ai-agent, practice]
sources: [2026/08/11/消费场景Agent实践论坛/01-邵俊-从 Demo 到 Production：高确定商业场景下的Agent实.pdf]
created: 2026-08-11
updated: 2026-08-11
---

# 高确定性商业场景下的 Agent 落地实践（运满满找货 AI）

2026 Agent 大会「消费场景Agent实践论坛」演讲《从 Demo 到 Production：高确定商业场景下的Agent落地实践》，演讲者邵俊（2026.7 深圳）。

## 业务背景：运满满找货 AI

运满满是中国最大数字货运平台，连接货主与卡车司机；找货 AI 帮司机在海量货源中高效找到合适的货。平台规模（数据来源：Full Truck Alliance（满帮集团）2025 年第三季度业绩公告）：

| 指标 | 数值 |
|------|------|
| 履约订单（2025 Q3） | 6340 万 |
| 发货方月活 MAU（平均） | 335 万 |
| 活跃卡车司机（近 12 个月） | 448 万 |
| 营收 RMB（2025 Q3） | 33.6 亿 |

为什么这门生意适合 Agent：

- **客单价高**：单笔运费金额可观，值得为每一单投入智能决策
- **交易非标**：货源千差万别、司机服务也非标，规则难以穷举，需理解决策
- **居间撮合提效**：可显著提升货与人的匹配效率
- **全流程线上化**：交易全程线上完成，数据闭环完整，决策可被反馈持续优化

## 三个核心约束与三个实践

| 约束 | 问题 | 对应实践 |
|------|------|---------|
| 感知必须确定且成本可控 | 自主感知无法确定"什么变了"；百万司机×持续 Loop = 成本扛不住 | 事件驱动，按需唤醒（Agent 不轮询，由业务事件确定性唤醒；不轮询 = 不花 token + 感知完全确定） |
| 推货必须快且成本可控 | 好货成交窗口分钟级；Agent 运行几十秒——好货早没了 | 意图编译为执行脚本（理解一次，编译一次，规则引擎秒级匹配；LLM 负责理解，确定性引擎负责执行） |
| 动作风险必须可控 | 不同动作风险等级完全不同；推错损伤信任，成交错单损失订金（取消订单 ➡ 扣订金） | 动作分层授权（不同动作不同信任层级；按风险分级，出错可审计、可回溯） |

## 运行流程：规划层预编译策略包，执行层按包执行

找货目标交互示例："长三角漫游，300 公里以内，13 米平板"。

- **策略校准触发源**：司机行为变化（换线路/拒推荐）、司机状态变化（位置变/装货完）、市场大盘变化（车货比骤变）、智能体自身异常（久未推/命中率低）
- **理解层（读懂司机）**：任务推断（这次要干什么——回家/赚钱/顺路）、条件提取（把司机说的话拆成结构化条件，如"这个司机不拉危险品、只跑高速"）、偏好学习、经验借鉴（司机脑回路+司机自己历史沉淀经验）；① 重新理解
- **规划层（分钟级·慢）**：目标翻译（把司机意图拆成可执行条件组合）、老司机经验融合（参考金牌司机怎么找货，结合当下市场行情）、策略包封装（打包成一份可直接执行的盯盘策略包）
- **盯盘策略包**（规划层预编译好的"如何找货+如何应对"决策包）：
  - 主匹配条件：起终点/价格/车型/时间窗
  - 价格弹性：可接受价格带+到价即推
  - 紧迫度规则：接近过期时的分级动作
  - 单票处理规则：被抢/改价/新货怎么处理
  - 操作权限：可自动议价/推送/接单的边界
- **执行层（实时盯盘·秒级快）**：按策略包逐票处理；单票货源变动即时匹配判断；评分入池；智能推送（够好就推，附可解释理由）；自动操作（权限范围内议价/接单）
  - 单票视角：新货上架 → 匹配 → 入池；货主改价 → 重新评分；货被抢了 → 单票剔除；即将过期 → 紧迫度升级
- **反馈闭环**：盯盘结果反馈（命中率/接单率）→ 触发策略校准（③ 盯盘结果回流）

核心逻辑：规划层把"金牌经验 + 市场行情 + 司机目标"预编译成策略包，实时盯盘只需按包执行，不做决策。

## 实践一：事件驱动与执行引擎

**问题**：Agent 不能 7x24 持续运行。百万司机 × 24h × 持续 Agent Loop = token 成本天文数字。但司机的找货任务是长程的（开启到结束至少持续数小时）——如何在不持续运行的前提下保持长程状态？

**方案：自定义事件类型 × Event2NL × 调度策略**

| 事件类型 | 触发时机 | 调度策略 | 说明 |
|---------|---------|---------|------|
| conversation | 司机发消息 | preempt（立即抢占） | 打断正在运行的 tick |
| tick | 周期性触发 | coalesce（合并丢弃） | 堆积多个只保留最新 |
| cold_start | 司机首次上线 | queue（排队等待） | 先到先服务 |
| intent_report | 意图变更上报 | queue | 触发策略重编译 |
| deal | 成交事件 | queue | 更新司机找活目标 |

**核心观点**：Agent 本质仍是长程的（有状态、有记忆、有持续目标），但它不"一直跑"——只在有事时被唤醒跑一次。好处：(1) 不轮询就不花 token；(2) 感知更可控——确定性地告诉 Agent"什么变了"。参考：Anthropic Agent Loop（evaluate → tool call → result → repeat），本实践改造为：repeat 由事件触发而非 Agent 自主循环。

**工程实现**：MQ 消费（多 topic 接入）→ 事件归一化（统一 DriverEvent 结构）→ 调度决策（打断/合并/排队）→ per-driver 队列（Redis sorted set）→ Worker 出队（触发 run_strategic）。

三个核心机制：

- **打断抢占（Preempt）**：司机发消息 → 写 cancel flag → 正在执行的 Worker 检测到 flag → 中止当前 run → 新事件入队优先执行；latency < 500ms（cancel 检测间隔）
- **coalesce 合并**：多个 tick 事件堆积时只保留最新一条、丢弃中间过程，避免无意义重复执行；效果：tick 风暴不会压垮系统
- **分布式串行保证**：同一司机同时只有一个 Worker 执行（pod 归属 + Redis 锁；锁续期 + 超时自动释放），保证状态一致性，无竞态，同 driver 串行执行

**实现结果**：事件驱动 → 统一调度入口 → 每个司机串行执行、互不干扰。Agent 每次被唤醒，已经知道"为什么醒来"和"上次停在哪"——event 提供 why，Redis state 提供 where。

## 实践二：意图编译为执行脚本（WatchPlan）

**问题**：好货成交窗口只有几分钟。Agent loop 运行几十秒且时间不可控。逐票让 Agent 判断"这票货适不适合"——永远抢不到。但司机的找货意图是自然语言表达的（如"我想跑上海到杭州冷链 价格别太低"）——如何用确定性规则秒级执行模糊意图？

**方案：两阶段分离（Slow Loop / Fast Loop）**

- **Slow Loop（慢循环）**：Agent + LLM 理解找货意图 → 编译为结构化规则脚本（WatchPlan）→ 写入 Redis，等待执行。时延容忍：分钟级。
- **Fast Loop（快循环）**：规则引擎拿着 WatchPlan 执行；每票新货源 → 规则匹配 → 评级 → 推送决策。时延要求：秒级，不依赖 Agent。

**核心观点**：Agent 的核心价值在于"理解司机找货意图并把决策逻辑编译为可执行规则"。LLM 负责理解——理解完了，就把接力棒交给确定性引擎。手脑分离。类比具身智能：目标 → 大脑 → 神经 → 肌肉。参考：Anthropic Dynamic Workflows——"moving the plan into code"（把意图编译为规则脚本，执行不依赖 Agent）。

**WatchPlan：约束 → 规则化编译产物**（输入示例："我想跑附近的冷链货，价格别太低，最好 30 公里以内能装货"）

- **召回条件（40+ 维度）**：出发地:上海、目的地:杭州、货物类型:冷链、装货距离:≤30km、价格下限:市场均价×0.8、车型匹配:冷藏车、时间窗:当前可用时段……共 40+ 维度，由 LLM 根据对话推断填充。作用：决定"看哪些货"。
- **过滤规则（阻断条件）**：C1 黑名单货主→直接拦截；C2 超重超限→拦截；C3 已推过未反馈→降权；C4 同线路饱和→限流；C5 司机已接单→暂停。每条规则独立判定，命中即阻断，无需模型介入。作用：决定"哪些不能推"。
- **执行脚本（策略参数）**：评级矩阵（完全匹配→自动推送、高度匹配→入池竞争、部分匹配→入池低优、不匹配→丢弃）；推送策略 TopN=3、间隔≥5min；门控参数：日推上限=20。作用：决定"怎么推、推多少"。

WatchPlan 是 Agent 每次"思考"的输出产物。只要找货策略不变，WatchPlan 不需要重新生成——规则引擎可以持续执行。

**Fast Loop 执行链路**：召回（按条件过滤货源）→ 过滤+评级（拦截/打分排序）→ 决策+入池（直推/其余竞争）→ 门控审核（频次/重复/状态）→ 推送（触达司机）。

执行特征：**确定性执行**（纯规则计算，结果可预期）、**可追溯+可观测**（每步有指标，问题定位到具体规则）、**可调参**（参数由 WatchPlan 控制，重编译即调整）。

**反馈闭环**（Fast Loop → tick → Strategic Loop）：推货结果产生（推了 N 票，司机未响应）→ tick 周期性感知（汇总推货反馈数据）→ Strategic Loop 重新理解（判断是否需要调整策略）→ WatchPlan 重编译（更新规则参数）。快慢 Loop 通过 tick 事件形成正向闭环——Fast Loop 负责执行，Strategic Loop 负责纠偏。

## 实践三：分层授权与能力治理

**问题**：Agent 能做的事越来越多——推货、打电话、帮加价、帮抢单，风险等级完全不同。如果所有动作都用同一套执行策略，要么过于保守（什么都不敢做），要么过于激进（出了错收不回来）。

**方案：5 级授权体系**

| 层级 | 名称 | 动作举例 | 执行策略 | 回退机制 |
|------|------|---------|---------|---------|
| L0 | 观察 | 记录行为、分析偏好 | 只读，不产生外部动作 | 无需回退 |
| L1 | 建议推送 | 货源入池 → 门控 → 推送 | 系统自动执行，门控拦截即停止 | 门控拦截即停止 |
| L2 | 直接推送 | A1 评级货源跳过池竞争直推 | 跳过池竞争，直接触达司机 | 频次超限 → 降级为 L1 |
| L3 | 主动外呼 | AI 电话通知司机优质货源 | 需司机预授权+时段限制+日次上限 | 司机投诉 → 关闭外呼 |
| L4 | 代操作 | 帮司机加价、帮司机抢单 | 每次执行前需司机实时确认（IM 弹窗） | 超时未确认 → 自动取消 |

当前状态：L0-L2 生产运行 · L3 灰度中 · L4 设计完成待落地。

**工程原则**：
1. 新能力默认从最严格层级开始，验证安全后逐级提升
2. 出问题随时降级——这是 Agent 能力扩展的"安全阀"
3. 完整记录决策链路，出错可审计、可追溯

**能力治理：评估驱动 × 能力演进**

评估体系（三层次）：
- 模块正确性（WatchPlan 对不对？Fast Loop 快不快？）：WatchPlan 合法率、编译稳定性、P99 时延
- 行为合理性（推得不好、太多、还是太慢？）：点击率、遗漏率、首次召回时延、曝N点1
- 业务有效性（Agent 帮司机赚到钱了吗？）：AI 成交占比、推N成1率、留存率、匹配增量成交率

能力演进路径：看货（信息推送，规则执行 · Agent 仅做意图理解）→ 盯盘（持续执行，规则 + Agent 协作 · 反馈闭环保证质量，推进中）→ 经营规划（自主决策，能力 + 信任双重门槛 · Agent 自主决策）。

对应关系：每开放一层新能力，先通过对应层级的评估验证安全性，确认后再放量。模块正确 → 行为合理 → 业务有效，能力从看货 → 盯盘 → 经营规划逐步放开。

## 结语

三个工程实践：事件驱动 Agent Loop（Agent 不持续运行，由业务事件唤醒、执行、休眠——解决：成本+感知的确定性）、意图编译为规则脚本（Agent 想明白一次，规则引擎重复执行千万次——解决：时延+成本+推货的确定性）、动作分层授权（不同动作不同信任层级，出错可审计、可回溯——解决：信任+可控）。

让 Agent 做判断，工程保证执行的确定性（可审计，可回溯）。

## 相关页面

- [[agent-architecture-patterns]] — Agent 架构模式
- [[ai-agent-ecommerce-content]] — AI Agent 在电商内容场景
- [[digital-employee]] — 数字员工概念
- [[agent-hook-governance]] — Hook 治理与护栏
