---
title: WorkBuddy 上下文工程实践
description: WorkBuddy 上下文工程五类动作（写入/选择/检索/压缩/隔离）、Prompt Cache 前缀复用策略、渐进式加载机制、意图识别路由——从产品侧看上下文如何被组织以提高模型决策正确率
aliases: [WorkBuddy上下文工程, WorkBuddy Context Engineering, 上下文工程五动作]
tags: [ai-agent, context-eng, practice]
sources: [2026/07/10/从模型到Harness：WorkBuddy如何把Agent做成可用产品.html]
created: 2026-07-10
updated: 2026-07-10
---

# WorkBuddy 上下文工程实践

## Context Engineering 定义

在一次模型决策前，设计**哪些信息进入上下文、以什么形式进入、放在什么位置、何时更新或移出**，以提高模型做出正确下一步决策的概率。

一个常见误区是"上下文窗口很大，全部放进去"。无关信息既占成本，也降低模型对当前重点的判断准确度。**Context Engineering 追求相关、准确、及时，不是单纯堆 token。**

## 五类动作

WorkBuddy 将上下文工程拆为五类动作，每类有精确含义：

| 动作 | 英文 | 精确含义 | 方向 |
|------|------|---------|------|
| 写入（Write） | Write | 把目标、规则、环境和任务状态**显式写进上下文**，别让模型靠猜 | 主动注入 |
| 选择（Select） | Select | 从已在手的候选信息里，**只挑当前这一步需要的**放进窗口 | Filter（过滤） |
| 检索（Retrieve） | Retrieve | 当前不在手的信息，从历史会话、资料库、工具目录里**按需捞进来** | Pull（拉取） |
| 压缩（Compress） | Compress | 长内容**外置到文件**、只留结论与证据位置，同时**清理窗口里过期或重复的内容** | 缩减 |
| 隔离（Isolate） | Isolate | 用独立会话或 Sub-agent 处理旁支任务，**只把结果带回主线**，避免污染主上下文 | 分流 |

这五类动作的核心思想：上下文不是被动累积的，而是被**主动设计和管理**的。

## Prompt Cache：上下文管理的第一要义

多轮对话每次都带上之前的上下文，全量计算成本过高。模型厂商会缓存已计算过的前缀，只计算新增部分，这就是 **Prompt Cache**。

它按**前缀匹配**，所以 WorkBuddy 遵循几条规则：

- **System Prompt、基础工具定义、长期规则放前面**，保持内容与顺序稳定
- **对话历史采用追加方式保存**，不修改已发送过的消息
- **当前文件、任务进度、时间、工具结果、新加载的 Skill 等动态内容追加到后面**
- **工具和 Skill 按需加载**，避免每轮重新生成并排列完整能力列表
- **只在上下文过长需要压缩、或纠正错误信息时**，才接受前缀变化和缓存重算

> 随着用户对积分（成本）越来越敏感，**缓存命中率正在成为被普遍关注的工程指标**。

### 上下文分层策略

WorkBuddy 采用分层策略，不让 System Prompt 承载所有信息：

| 层 | 存放内容 | 特点 |
|----|---------|------|
| System Prompt | 所有任务都适用的角色与安全要求 | 稳定、前置、高优先级 |
| Workspace 规则文件 | 项目规范（架构、依赖、安全、风格） | 按项目作用域 |
| Skill | 某类任务的步骤、约束和验收标准 | 按需加载 |
| 动态上下文 | 当前请求、进度、工具结果、环境信息 | 追加到后方 |

## 渐进式加载

### 工具定义过多时

每个 Tool Schema 都占上下文，工具越多、语义越重叠，模型选择越困难。WorkBuddy 采用**分阶段的能力发现机制**：

1. 先让模型看到工具**名称和简要描述**
2. 再根据任务需要**加载更具体的工具说明**
3. 同时提供**工具检索能力**

### Skill 同样渐进

Skill 也用同样机制：先看名称和描述 → 确认适用后再读完整 SKILL.md → 需要时再打开参考资料与脚本。

### MCP 同理

高质量的 MCP 同样可以把要用的资源拉取到本地、提供 search 能力，减少上下文注入。

> **几乎所有外接能力都可以按这个思路组织：默认只暴露名称和简介，真正进入某个任务时再加载完整内容。** 上下文里始终只保留当前需要的能力，而不是把所有能力一次性铺开。

### 意图识别是前置环节

用户的一句话背后可能对应问答、改代码、检索资料或调用外部系统等不同任务。系统先识别意图，再据此决定加载哪一类工具、Skill 和 MCP，让后续上下文只包含与当前任务相关的能力。

> 意图识别负责"先选对方向"，渐进式加载负责"再按需展开"，两者共同控制上下文规模。

## 工具结果截断策略

工具结果过长时，WorkBuddy 给每个 Tool Result 设截断策略：超出时分页、截断或写入文件。

截断时的关键原则：**明确告诉模型"结果未完整"**，附上总量、截断位置和继续读取方法。否则模型会把前 100 条误当成全部。

错误返回也不只返回 error 或一段堆栈，还要返回：
- 失败原因
- 可修正参数
- 是否可重试
- 建议下一步

## 产品需要组织的环境上下文

WorkBuddy 在每次请求前组织以下信息，这是 Agent 给出贴合当前环境结果的前提：

- 操作系统（Windows/Mac/Linux）
- Shell 类型（bash/powershell）
- 用户当前时间、时区、地理位置（模型不感知时间）
- 当前打开的文件
- 产品语言
- 当前仓库规则和未完成任务
- 已安装的 Skills
- 已连接的 Connectors

## 相关页面

- [[workbuddy-agent-product-design]] — WorkBuddy Agent 完整产品设计
- [[google-context-engineering]] — Google 上下文工程白皮书
- [[manus-context-engineering]] — Manus 上下文工程实践
- [[claude-md-best-practices]] — CLAUDE.md 最佳实践
- [[agent-memory-system]] — Agent 长短期记忆系统
- [[agent-harness-overview]] — Agent Harness 综述
